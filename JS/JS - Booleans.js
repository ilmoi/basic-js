// --------------------------------- BOOLEANS
true / false //instead of True / False
== //tries to convert both to the same type
=== //does not perform type conversion
3 ===  3   // true
3 === '3'  // false

// same goes for inequality
3 !==  3   // false
3 !== '3'  // true
4 !==  3   // true

// test two statements at the same time
(num > 5 && num < 10) //vs "and" or "&" in python
(num > 10 || num < 5) //or
